import App from '../App.js';
import SignIn from '../SignIn.js';

var indexRoutes = [
    { path: "/", name: "sign_in", component: SignIn },
    { path: "/", name: "sign_up", component: SignUp }
];

export default indexRoutes;